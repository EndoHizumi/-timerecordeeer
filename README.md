# timerecordeeer

## 概要
freee用打刻自動化スクリプト

## 事前準備

1. requestsのインストール
   1. `pip install requests` を、実行する。
2. 

## 使い方

```bash

$ python timerecordeeer.py find yyyy-mm-dd 

$ python timerecordeeer.py emboss [clock_in|break_begin|break_end|clock_out]
```